Price<br/>
<span class="format_long_nums"><%= @price_test %></span>
<br/>
<br/>
<span class="format_long_nums"><%= @price_test2 %></span>
<br/>
<br/>
<span class="format_long_nums"><%= @price_test3 %></span>
<br/>

Form<br/>

<%= form_for @car, :url => test_post_adverts_path, :method => "post" do |f| %>
<br><br>

    <label>Minimum number of beds</label>
    <%= f.text_field :name %>
    <div id='slider'></div>
    <br><br>
       <%= radio_bootstrap_tag(@engines1, @car.engine_id, 'engine') %>
    <br><br>
       <%= radio_bootstrap_tag(@engines2, @car.engine_id, 'engine2') %>
    <br><br>
       <%= checkbox_bootstrap_tag(@engines1, 'engine3') %>
    <br><br>
    <br><br>
       <%= checkbox_bootstrap_tag(@engines2, 'engine4') %>
    <br><br>
    <%= f.submit "Test Post", :class => "btn btn-inverse" %>
<% end %>

<br><br><br>

 <form id="commentForm2" method="post">
 	<p>
 		<label for="years">Year: </label>
 	  <input id="years">
 	</p>
 	<p>
 		 <label class="control-label" for="num2">Your Name (min 5 char)</label>
  </p>
  <p>
    <input id="num2" class="required" minlength="5" />
  </p>
  <p>
     <input class="submit" type="submit" value="Submit"/>
  </p>
 </form>



<b>New adv</b>
<br/><br>
<%= @models %>
<br/><br>
<%= render 'form_city' %>
<br/><br>
<a href="#brand_modal" role="button" data-toggle="modal" id='a_brand_name'>Бренды</a>
<div id="brand_modal" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <ul>
    <% @all_brands.each do |brand| %>
      <li>Name: <%= link_to brand.name, get_models_by_brand_adverts_path(:brand_id => brand.id, :brand_name => brand.name), :remote => true, :method => :post %></li>
    <% end %>
  </ul>
</div>
<br/><br>
<a href="#model_modal" role="button" data-toggle="modal" id='a_model_name' style="display: none;">Модели</a>
<div id="model_modal" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div id='div_models'></div>
</div>
<br/><br>

<div id='div_category_name'></div>

<br/><br>
Форма<br>
<div id='div_form'></div>
<br/><br>

<br/><br>
All Items
<ul>
  <% @all_cars.each do |ac| %>
    <li>ID: <%= ac.id %>, Name: <%= ac.name %>, Model_ID: <%= ac.model_id %>, City_ID: <%= ac.city_id %>, User_ID: <%= ac.user_id %>, created: <%= ac.created_at %>, updated: <%= ac.updated_at %>, engine: <%= ac.engine_id %></li>
  <% end %>
</ul>

<br/><br>

Все категории
<br/><br>
  <ul>
    <% @all_categories.each do |c| %>
      <li>Name: <%= link_to c.name %></li>
    <% end %>
  </ul>

<br/><br>
by category
<ul>
  <% @models.each do |m| %>
    <li>Name: <%= m.name %>, brand id: <%= m.brand_id %>, category id: <%= m.category_id %></li>
  <% end %>
</ul>

<ul>
  <% @brands.each do |b| %>
    <li>Name: <%= b.name %></li>
  <% end %>
</ul>



<br/><br><br/><br>
<ul>
  <% @all_models.each do |m| %>
    <li>Name:<%= m.name %>, brand id: <%= m.brand_id %>, category id: <%= m.category_id %></li>
  <% end %>
</ul>
