
<li>Area: <%= item.id %>,
	<%= link_to item.name,
            set_custom_area_user_registration_path(:area_id => item.id, :area_name => item.name),
            :remote => true, :method => :put %>
  